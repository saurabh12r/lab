/**
 * API client placeholder. Replace with real fetch/axios when backend is ready.
 * Example: export async function get<T>(path: string): Promise<T> { const res = await fetch(`/api${path}`); return res.json(); }
 */

export const API_BASE = "/api";
